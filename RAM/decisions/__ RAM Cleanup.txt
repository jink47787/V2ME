political_decisions = {

    end_war_nec = {
        picture = marching_soldiers
        potential = {
            ai = yes
			NOT = { has_global_flag = daily_cleanup_on }
        }

        allow = {
        }

        alert = no

        effect = {
			set_global_flag = daily_cleanup_on
            CLN = { country_event = 477877787 }
        }
        ai_will_do = { factor = 1 }
    }
}
